<ul class="mt-3 nav nav-pills nav-fill">
    {% for category in categories %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('show_category', {'slug': category.slug}) }}">{{ category.title }}</a>
        </li>
    {% endfor %}
</ul>
<hr>
